# If a bundle needs ncurses please add the ncurses bundle in the bundle_deps file. Also
# add the extra CFLAGS an LDFLAGS to the build environment so the package can find
# the include files and libraries.
#
#    CFLAGS="-I$WORK_DIR/overlay/ncurses/ncurses_installed/usr/include"
#    LDFLAGS="-L$WORK_DIR/overlay/ncurses/ncurses_installed/usr/lib"
#
# You can find the latest ncurses archives here:
#
#   https://ftp.gnu.org/pub/gnu/ncurses/
#
NCURSES_SOURCE_URL=https://ftp.gnu.org/pub/gnu/ncurses/ncurses-6.1.tar.gz
